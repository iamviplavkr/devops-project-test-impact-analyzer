<<<<<<< HEAD
name: CI/CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Setup Python
      uses: actions/setup-python@v3
      with:
        python-version: 3.9

    - name: Install Dependencies
      run: pip install -r requirements.txt

    - name: Run Tests
      run: python -m pytest tests/
=======
name: DevOps CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-impact-analysis:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Checkout code
    - name: Checkout Repository
      uses: actions/checkout@v3

    # 2️⃣ Setup Python
    - name: Setup Python
      uses: actions/setup-python@v3
      with:
        python-version: '3.9'

    # 3️⃣ Install dependencies
    - name: Install Dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    # 4️⃣ Run full tests (baseline)
    - name: Run All Tests
      run: pytest tests/

    # 5️⃣ Run Test Impact Analysis (MAIN FEATURE 🔥)
    - name: Run Impacted Tests Only
      run: python src/scripts/run_tests.py

    # 6️⃣ Build Docker Image (Optional but high marks)
    - name: Build Docker Image
      run: docker build -t test-impact-analyzer .

    # 7️⃣ Success message
    - name: Pipeline Success
      run: echo "CI/CD Pipeline executed successfully!"
>>>>>>> d4e1be3 (Updated login feature)
